# ─────────────────────────────────────────────────────────────────────────────
# DEPRECATED — Phase 4 monolith CD workflow
# ─────────────────────────────────────────────────────────────────────────────
# Phase 5 migrated to per-service CD workflows:
#   ci-cd-fetcher.yml      → fetcher-service (Docker → ghcr.io/journalist-fetcher)
#   ci-cd-summarizer.yml   → summarizer-service (Docker → ghcr.io/journalist-summarizer)
#   ci-cd-notification.yml → notification-service (Docker → ghcr.io/journalist-notification)
#   ci-cd-gateway.yml      → gateway-service (Docker → ghcr.io/journalist-gateway)
# ─────────────────────────────────────────────────────────────────────────────

name: CD — Build & Push Docker Image (DEPRECATED — Phase 4 monolith)

on:
  push:
    branches: [ "DISABLED" ]   # effectively disabled
    paths:
      - 'src/**'               # monolith src/ — no longer used in Phase 5

jobs:
  docker-build-push:
    runs-on: ubuntu-latest
    name: "[DEPRECATED] Build & Push — monolith"
    steps:
      - run: echo "This workflow is deprecated. See ci-cd-fetcher.yml etc."
