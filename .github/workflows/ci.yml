# ─────────────────────────────────────────────────────────────────────────────
# DEPRECATED — Phase 4 monolith CI workflow
# ─────────────────────────────────────────────────────────────────────────────
# Phase 5 migrated to per-service workflows with path filters:
#   ci-cd-fetcher.yml      → fetcher-service
#   ci-cd-summarizer.yml   → summarizer-service
#   ci-cd-notification.yml → notification-service
#   ci-cd-gateway.yml      → gateway-service
#
# This file is kept for reference only and will NOT trigger (disabled via paths).
# ─────────────────────────────────────────────────────────────────────────────

name: CI — Build & Test (DEPRECATED — Phase 4 monolith)

on:
  push:
    branches: [ "DISABLED" ]   # effectively disabled — no branch named DISABLED
    paths:
      - 'src/**'               # monolith src/ — no longer used in Phase 5

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    name: "[DEPRECATED] Build & Test — monolith"
    steps:
      - run: echo "This workflow is deprecated. See ci-cd-fetcher.yml etc."
